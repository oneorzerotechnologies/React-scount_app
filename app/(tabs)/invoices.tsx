import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function InvoicesScreen() {
  return (
    <PlaceholderScreen
      step="06A · LIST"
      title="Invoices"
      subtitle="Invoice tracker lands here next — same list pattern as Quotations with overdue tinting. Spec in docs/UI-DESIGN.md §6."
    />
  );
}
