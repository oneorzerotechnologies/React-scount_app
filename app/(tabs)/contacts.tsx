import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function ContactsScreen() {
  return (
    <PlaceholderScreen
      step="07A · LIST"
      title="Contacts"
      subtitle="Unified clients + suppliers directory with a segmented control on top. Spec in docs/UI-DESIGN.md §7."
    />
  );
}
